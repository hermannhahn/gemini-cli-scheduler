{
	"name": "gemini-cli-scheduler",
	"version": "1.1.2",
	"description": "Powerful extension for scheduling tasks, shell commands, and reminders. Supports Jules, Gemini, and Ollama executors with relative time support.",
	"keywords": [
		"Scheduler",
		"Gemini",
		"CLI"
	],
	"homepage": "https://github.com/hermannhahn/gemini-cli-scheduler#readme",
	"bugs": {
		"url": "https://github.com/hermannhahn/gemini-cli-scheduler/issues"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/hermannhahn/gemini-cli-scheduler.git"
	},
	"license": "ISC",
	"author": "Hermann Hahn",
	"type": "commonjs",
	"main": "dist/tool_code.js",
	"files": [
		"dist",
		"gemini-extension.json"
	],
	"scripts": {
		"lint": "eslint \"src/**/*.ts\" --fix",
		"test": "echo \"No tests specified\" && exit 0",
		"webpack": "webpack",
		"build": "webpack",
		"cleanup-releases": "node scripts/cleanup-releases.js",
		"update:version": "npm run lint && npm version patch --no-git-tag-version && node scripts/update-version.js && webpack && npm run push:version",
		"push:version": "git add . && node -e \"const v = require('./package.json').version; require('child_process').execSync('git commit -m \\\"chore: bump version to ' + v + '\\\"', {stdio: 'inherit'})\" && git push origin development",
		"push:release": "node -e \"const v = require('./package.json').version; require('child_process').execSync('gh release create v' + v + ' --generate-notes', {stdio: 'inherit'})\"",
		"deploy": "node scripts/deploy.js"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.28.0",
		"node-schedule": "^2.1.1"
	},
	"devDependencies": {
		"@eslint/js": "^10.0.1",
		"@types/node": "^20.19.37",
		"@types/node-schedule": "^2.1.8",
		"eslint": "^10.0.3",
		"ts-loader": "^9.5.4",
		"typescript": "^5.9.3",
		"typescript-eslint": "^8.57.1",
		"webpack": "^5.105.2",
		"webpack-cli": "^6.0.1"
	}
}
