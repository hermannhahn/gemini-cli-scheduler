{
	"name": "gemini-cli-scheduler",
	"version": "0.8.25",
	"description": "A powerful, persistent task scheduler for Gemini CLI. Schedule shell commands to run at specific times, manage recurring tasks, and control sub-agent (Jules) daily usage quotas.",
	"mcpServers": {
		"scheduler": {
			"command": "node",
			"args": ["${extensionPath}/tool_code.js"]
		}
	}
}
