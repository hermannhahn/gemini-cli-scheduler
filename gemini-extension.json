{
	"name": "gemini-cli-scheduler",
	"version": "1.0.8",
	"description": "Powerful task scheduler and reminder system for Gemini CLI. Supports scheduling shell commands, sub-agent (Jules) tasks, and specific models with relative time support ('in 5 minutes').",
	"mcpServers": {
		"scheduler": {
			"command": "node",
			"args": ["${extensionPath}/dist/tool_code.js"]
		}
	}
}
